<%= render'application/sidebar' %>
<div class = "content_index">
  <div class = "content_inner">
    <% if notice %>
      <p class="alert alert-notice"><%= notice %></p>
    <% end %>
      <h3>みんなの投稿一覧</h3>
    <div class="search_box">
      <%= search_form_for @q, url: search_posts_path do |f| %>
        <ul class ="ulul">
          <li><%= f.label :address_cont, '住所' %></li>
          <li><%= f.search_field :address_cont %></li>
          <li><%= f.submit  '検索'%></li>
        </ul>
      <% end %>
    </div>
        <%= paginate @posts %>

    <div class= 'content_target'>
      <% @posts.each do |post| %>
        <%= link_to post_path(post), class:' post-color','data-turbolinks': false do %>
          <div class ="index_container alert alert-info ">
            <p><%= image_tag(post.image.to_s) %></p>
            <ul class = "sid_content">
              <li class="text-break">タイトル:<%= post.title %></li>
              <li class="text-break"><%= "コンテント:#{post.content}" %></li>
            </ul>
          </div>
        <% end %>
      <% end %>
    </div>
  </div>
</div>

