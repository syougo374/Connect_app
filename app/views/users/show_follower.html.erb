<%= render'application/sidebar' %>
<div class = "content_index">
  <p>フォロー一覧</p>
  <% @users.each do |user| %>
    <div class = "follow_box">
      <%= image_tag(@user.image.to_s) %>
      <%= user.name %>
    </div>
  <% end %>
</div>
</div>